{% extends 'base.html.twig' %}

{% block body %}
    <h1>Erreur {{ code }}</h1>
    <h2>{{ message }}</h2>

    <p>
        <a href="{{ path('app_index') }}">Retour à l'accueil dans 3 secondes</a>.
    </p>
{% endblock %}

{% block javascripts %}
    <script>
        setTimeout(function () {
            document.location.href = "{{ path('app_index') }}";
        }, 3000);
    </script>
{% endblock %}
