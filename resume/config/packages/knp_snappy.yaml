knp_snappy:
    pdf:
        enabled:    true
        binary:     '%kernel.root_dir%%env(WKHTMLTOPDF_PATH)%'
        options:    []
    image:
        enabled:    true
        binary:     '%kernel.root_dir%%env(WKHTMLTOIMAGE_PATH)%'
        options:    []
